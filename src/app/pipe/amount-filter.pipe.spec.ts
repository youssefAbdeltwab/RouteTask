import { AmountFilterPipe } from './amount-filter.pipe';

describe('AmountFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
